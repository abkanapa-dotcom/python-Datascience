'''sentence = "Python is powerful for data science"
# 1️⃣ Print the first character
print(sentence[0])
# 2️⃣ Print the last character
print(sentence[-1])
# 3️⃣ Print the character at index 7
print(sentence[7])
# 4️⃣ Print the character at index -5
print(sentence[-5])

# 5️⃣ Extract "Python"
print(sentence.split()[0])
# 6️⃣ Extract "powerful"
print(sentence.split()[1])
# 7️⃣ Extract "data"
print(sentence.split()[2])
# 8️⃣ Extract "science"
print(sentence.split()[3])
'''
# (start:stop:step)
'''
print(sentence.lower())
print(sentence.upper())
print(sentence.title())
print(sentence.capitalize())
print(sentence.strip(" "))

#Q1. Remove extra spaces from the string below and normalize it to a single space between words.
s = "   Data   Analyst   Role   "
print(" ".join(s.split()))
# print(s.strip(" "))
# Q2. Convert the following string to title case.
s = "pYtHon dATa sCIEncE"
print(s.lower().title())'''
# Q3. From the list below, extract only values that are purely numeric.
'''values = ["123", "45a", "678", "9.8"]
v_digit= list((i if i.isdigit() else "" for i in values))
v_digits = list(i for i in values if i.isdigit())
print(v_digit)
print(v_digits)'''
# Level 2: Real-World Data Analyst Tasks
# Q4. Count word frequency in the given sentence.
'''s = "python python data python analyst data"
freq = {}
freq_split = s.split()
for i in freq_split:
    if i in freq:
        freq[i]=freq[i]+1
        print(freq)
    else:
        freq[i]=1
        print(freq)
print(freq)'''
# Q5. Extract domain names (without .com, .in) from the email list.
'''emails = ["user1@gmail.com", "admin@yahoo.com", "hr@amazon.in"]
email = "".join(emails)

print("".join(emails).split("@")[1].split(".")[0])
print(list((email.split("@")[1].split(".")[0] for email in emails)))'''
# Q6. Remove currency symbols and convert prices to integers.
prices = ["$120", "₹450", "€300"]
# Q7. Clean product names:

# Remove special characters
# Convert to lowercase
# Replace separators with space
products = [" iPhone-14 ", "Samsung_Galaxy", " OnePlus-11 "]
# Level 3: Analytics & Interview-Oriented

# Q8. Extract year from each date string.
dates = ["2023-05-10", "2022-11-01", "2021-01-25"]
# Q9. Identify valid phone numbers (10 digits only).
phones = ["9876543210", "12345", "98765abc10", "9999999999"]
# Q10. Mask sensitive card information leaving only last 4 digits visible.
data = "User card number is 4567-8912-3456"
# Q11. Extract hashtags from the text (without #).
text = "Loving #Python and #DataAnalytics for #CareerGrowth"
                                     
#                                      Level 4: Feature Engineering & SQL Thinking
# Q12. Remove duplicate words while preserving order.
s = "data data science science python"
# Q13. Split full names into first and last names.
names = ["Ekta Negi", "Rahul Sharma", "Anita Singh"]
# Q14. Categorize reviews into positive, negative, or neutral.
reviews = ["excellent product", "bad quality", "average item"]
# Q15. Remove stopwords (is, a, this) from the sentence.
s = "this is a data analyst interview question"
